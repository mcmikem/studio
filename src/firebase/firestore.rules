rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for development. Allows any authenticated user to read/write.
    // WARNING: Not secure for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
